import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 0) {
            // Generated Content
            {{CONTENT}}
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
