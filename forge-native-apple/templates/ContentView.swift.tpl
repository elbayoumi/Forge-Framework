import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 0) {
            // Generated Content
            {{CONTENT}}
        }
        .frame(minWidth: 400, minHeight: 300)
        .padding()
        .background(Theme.backgroundColor)
    }
}

#Preview {
    ContentView()
}
